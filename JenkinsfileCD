def skipdelta(){

		if(params.FUll_Deployment == false){
			isDelta == true

		}

		return isDelta
	}

pipeline {
    agent any
	parameters {
            booleanParam (
                defaultValue: false,
                description: 'Select only when you want to do full Deployment',
                name : 'FUll_Deployment')
        }
	environment {
		currentBuildNumber = "${env.BUILD_NUMBER}"
	}

	tools {
            maven 'Maven 3.6.3'
            jdk 'jdk8'

     }
    stages {
        stage('Full Deploy') {
        when {
                        expression {
                            return  params.FUll_Deployment
                        }
        }
            steps {
                echo 'Building..'
                sh '... Deploying Full... '
            }
        }
        stage('Delta Deploy') {
         when {
                                expression {
                                	skipdelta()
                                    return  isDelta
                                }
           }
            steps {
                echo 'Testing..'
                sh '... Deploying Delta... '
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                sh '...Testing Deploy... '
            }
        }
     }
}
